
syntax = "proto2";

package example;

enum FOO { X = 17; };

message Test {
  reserved 4;
  required string label = 1;
  optional int32 type = 2 [default=77];
  repeated int64 reps = 3;
  optional int32 num = 5;
}